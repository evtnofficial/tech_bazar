/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"drive.google.com",
			"google.com",
			"www.thoughtco.com",
			"i0.wp.com",
		],
	},
};

export default nextConfig;
